{
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  },
  "scripts": {
    "start": "concurrently \"cd client && cross-env npm run dev\" \"cd server && set && npm start\"",
    "start:client": "cd client && cross-env PORT=5000 npm run dev",
    "start:server": "cd server && PORT=5000 npm start"
  },
  "dependencies": {
    "nodemon": "^3.1.0"
  }
}